[package]
name = "voice-intent"
edition.workspace = true
version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

# No dependencies: stage 1 is a hand-rolled table-driven matcher (no regex
# crate needed — the pattern set is small and fixed), stage 2 is a trait
# boundary only, and CommandBias is pure string plumbing. All std.
[dependencies]
